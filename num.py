import pandas as pd
import numpy as np
dates = pd.date_range(start="2022-04-01",
           end="2022-04-30")

rug = np.random.default_rng(123)
df = pd.DataFrame(rug.integers(1, 31, size=30),
          index = dates,
          columns=["乱数値"])

objs = [df_202205_fillna]  # Define the "objs" variable with the desired DataFrame(s)

pd.concat(objs, axis=0,
          
          
 "Wait a moment, I'm tired so I'm taking a week off."
 
ax.plot(x, y, linewidth=10, color="red", linestyle="dashed")